@model IEnumerable<AplicacionBase.Models.Comment>

@{
   Layout = "";
}
@if (Model.Count() > 0)
{
    foreach (var item in Model)
    {
         <div class="comentario">@item.User.FirstNames @item.User.LastNames dice: @item.Content</div>
    }
}


